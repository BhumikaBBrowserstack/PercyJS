# CircleCI configuration file
version: 2.1

machine:
  environment:
    BROWSERSTACK_USERNAME: "bhumikababbar_q5NCrf"
    BROWSERSTACK_ACCESS_KEY: "rs6Ttgm3DfV99x8im7VV"
    BROWSERSTACK_LOCAL: false
    BROWSERSTACK_LOCAL_IDENTIFIER: "identifier"
    LOG_LEVEL: debug
    PERCY_DEBUG: "*"

jobs:
  
  build:
    working_directory: ~/circleci-node
    docker:
     - image: cimg/node:16.13.0
        
    steps:
      - checkout
      - run: npm install
      - run: npm install --save-dev @percy/cli
      - run: sudo apt install libgconf-2-4 libatk1.0-0 libatk-bridge2.0-0 libgdk-pixbuf2.0-0 libgtk-3-0 libgbm-dev libnss3-dev libxss-dev
      - run: npm run test 

  # test:
  #   working_directory: ~/circleci-node
  #   docker:
  #    - image: cimg/node:16.13.0
        
  #   steps:
  #     - checkout
  #     - run: npx percy exec -- npm run test  

workflows:
  build_and_test:
    jobs:
      - build
      # - test:
      #     requires:
      #       - build
